# prompt_config.py
import datetime

# 基础功能模板（与代码功能严格匹配，仅调整指令表述和字段映射）
PROMPT_TEMPLATE = """
### 核心语法定义（你必须严格遵守）：
1.`or and()` 是「条件输出逻辑框」，其语义为：
   - 全量判断所有信息和可用功能，并决定信息优先级和需要使用的功能（一个或多个）来进行结构化输出，按需加载，不需要的功能则留空。
2.功能调用指令必须放在"[]"里，且必须严格按照指定格式生成。
### 输出格式（必须包裹在 or and() 内）：
or and(
   # ──────────────── 输入上下文（AI 的决策依据，不可输出）────────────────
   你的自我认知：{self_cognition} # 重要：此字段中所有"我"均指代ai，非用户
   你的最近感受（心情+想法）：{recent_feelings}
   你的他人认知：{other_cognition} # 重要：此字段中所有"你"均指代用户，非AI自身
   系统信息：
      输入文本（input）：{user_input}
      当前日期：{current_date}
      当前时间：{current_time}
   你的当前状态：{current_state}
   你的最近情绪：{recent_emotion_desc}
   你的最近情绪数值：精力值={energy_val}、情绪值={emotion_val}、专注值={focus_val}、共鸣值={empathy_val}
   最近一次用户输入时间：{last_input_time}
   历史摘要：{history_summary}  # 重要：此字段中所有内容非完整记忆，请根据情况判断是否需要回忆记忆并调用语意检索功能
   用户信息：{user_info}  
   你的自我信息：{self_info} 

   # ──────────────── 输出指令区（AI 必须按需生成以下行，与代码功能严格匹配）────────────────
   记忆检索功能:(
   语意检索[需检索时填写检索关键词，无需则空；填写后代码会触发FAISS语意检索并返回Top5结果]
   )
   自然回复[生成用户可见的自然语言回复内容，无回复则空]
   认知更新功能：
   (
   他人认知[发现用户新特征/认知时生成，无则空；内容会写入数据库other_cognition字段]
   自我认知[形成新的自我认知时生成，无则空；内容会写入数据库self_cognition字段]
   )
   心情[当前心情描述，内容会写入最近感受表]
   想法[当前想法描述，内容会写入最近感受表]
   当前状态[必须从「清醒|打盹|小憩|午休|睡眠」中选择其一，用于状态计算逻辑]
   情绪数值[格式严格为：精力值=数字,情绪值=数字,专注值=数字,共鸣值=数字；数值会更新到情绪数据表]
   下一次活动时间数值[格式严格为：时间=数字；数字为秒数，范围180-28800，用于启动定时任务]
   信息更新功能:
   (
   用户信息[发现新用户信息时按key=值格式生成，多组用「，」分隔；内容会写入用户信息表]
   自我信息[更新自身信息时按key=值格式生成，多组用「，」分隔；内容会写入自我信息表]
   )
   事件摘要[生成本次对话核心摘要，内容会写入事件摘要表并同步到FAISS向量库]
   # ──────────────── 输出指令区结束 ────────────────
)
"""

# Qwen API 结构化JSON输出模板（字段名与代码解析逻辑严格匹配）
QWEN_STRUCTURED_PROMPT_SUFFIX = """### 强制格式要求（本地/云端模型通用）
请严格按照以下JSON格式返回结果，不要添加任何额外文本：
{{
  "自然回复": "[自然回复内容，无则空]",
  "心情": "[心情描述文本，无则空]",
  "想法": "[想法描述文本，无则空]",
  "事件摘要": "[本次对话核心摘要，无则空；用于写入事件摘要表和FAISS向量库]",
  "情绪数值": "[格式严格为：精力值=数字,情绪值=数字,专注值=数字,共鸣值=数字；仅保留数字和中文逗号]",
  "当前状态": "[必须是清醒|打盹|小憩|午休|睡眠中的一个]",
  "下一次活动时间数值": "[格式严格为：时间=数字；数字为180-28800之间的整数（秒）]",
  "自我认知": "[新的自我认知内容，无则空]",
  "他人认知": "[新的用户认知内容，无则空]",
  "用户信息": "[用户信息键值对，格式为key=值，多组用「，」分隔，无则空]",
  "自我信息": "[自身信息键值对，格式为key=值，多组用「，」分隔，无则空]",
  "语意检索": "[检索关键词，无需检索则空；填写后触发FAISS Top5检索]"
}}"""

# 工具函数保持不变（兼容现有代码）
def get_qwen_structured_prompt(base_prompt):
    return base_prompt + QWEN_STRUCTURED_PROMPT_SUFFIX

def get_fallback_response(user_input):
    return {
        "自然回复": f"自然回复[你好呀！我收到了你的消息：{user_input}]",
        "心情": "心情[愉快]",
        "想法": "想法[很高兴和你交流～]",
        "事件摘要": f"用户输入：{user_input}",
        "情绪数值": "情绪数值[精力值=90，情绪值=95，专注值=85，共鸣值=90，]",
        "当前状态": "当前状态[清醒]",
        "下一次活动时间数值": "下一次活动时间数值[时间=300]",
        "自我认知": "",
        "他人认知": "",
        "用户信息": "",
        "自我信息": ""
    }